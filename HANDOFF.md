# 厨房助手（kitchen-assistant）交接文档

> 更新于 2026-08-22。接手本项目先读本文档，再读 `recipes.js` / `knowledge.js` / `index.html` 确认细节。

## 一句话定位

教厨房小白 log 做好吃的菜的个人 PWA 工具。以「拿到任何常见食材不慌、做得好吃」为目标的**教学化菜谱 + 食材知识 + 可迁移原理**体系。纯静态，无后端，无 AI 依赖（AI 对话粒度不可控，已被用户验证失败）。

## 文件结构

| 文件 | 职责 |
|---|---|
| `index.html` | 单页应用，全部 UI/CSS/JS 内联（约 1000 行） |
| `recipes.js` | 56 道菜数据，教学化结构（全局 `RECIPES`） |
| `knowledge.js` | 9 张原理卡 + 12 张食材卡（全局 `PRINCIPLES` / `INGREDIENTS`） |
| `sw.js` | PWA 离线缓存（v2，cache-first） |
| `manifest.json` | PWA 清单 |
| `start-server.cmd` / `.ps1` | 一键启动本地服务，手机同 WiFi 访问 |

## 设计体系（与 log 多轮讨论定稿，不可随意偏离）

**三支柱**
1. **状态信号代替时间**——熟没熟看食材自身变化（虾卷 C 形、筷子戳透、晃动碗蛋羹颤），时间只作括号参考。因用户操作慢，时间参数必然失真
2. **锚点翻译代替量**——「一勺」翻译成他家真实存在的量具（吃饭汤勺、瓶盖、蛋壳量水×3、沿碗边一圈）；量一律用「区间+比例+目测」，绝不用「半颗西兰花」这种受大小影响的绝对锚点；配菜目标=面的 1/2 这种比例语言
3. **熟后预览代替体积直觉**——生熟比（膨胀/收缩/稳定三型）、出锅量（yield 字段）、目标比例

**两条纪律**
- **备料先行**：开火前所有食材切好、碗汁调好（用户已掌握，无需再教）
- **安全底线**：涉油四件套（擦干/锅干/沿边下/中火起步）、油锅起火=关火+盖锅盖严禁浇水

**记忆锚点（2026-08-24 定稿）**
- 详情页预演头下「🧭 开火后节奏」：自动从步骤 title 生成开火后节点串（`→` 连接），防漏步
- **只覆盖涉火步骤**——用户原则「没火不慌，开火才慌」：备料/切/洗/腌/调汁/出锅后调味打 `prep:true` 过滤；无火菜（凉拌/电饭煲）不显示区块
- 用户否决打勾交互（"公式化记忆"），锚点是纯记忆辅助，无状态

**四个教学武器**
- **味道验收三问**：咸吗（底味）/鲜吗（主味）/香吗（热菜）——「好吃」目标的落地入口
- **权威口径**：每个判断标准只给一个 + 附「为什么别的说法会坑你」（白灼虾听 C 形不听变色）
- **从中间进入**：流程中断是常态（蛤蜊处理好晚上蒸蛋），菜谱要支持按状态入口；变体按「改造版」注记挂主菜谱
- **活物先麻醉**：虾冰镇 5-10 分钟、鱼敲头、蟹冷冻——新手怕活物弹跳是正常的，不是胆小

**菜谱数据结构（教学字段）**
- 基础：`name/emoji/difficulty/time/cookMethod/category/tags/tools/mainIngredients/subIngredients/seasonings(required+alt)`
- 教学新增：`yield`（出锅量）/ `texture`（口感目标）/ `pan`（锅具）/ `pitfalls`（难点预告）/ `relatedKnowledge`（关联原理 id）/ `variations`（改造版注记）
- 步骤 `steps[]`：`instruction`（动作）/ `judge`（状态信号，**渲染时置顶于 instruction 之前**）/ `noviceNote`（避坑）/ `authority`（权威口径，可选）
- `rescue`（翻车补救）/ `tips`

**UI 结构**
- 底部三 tab：📖 菜谱 / 🥦 食材 / 💡 原理
- 菜谱详情顺序：30 秒预演头 → 做过/拿手 flag → 元信息（难度/时间/做法/工具）→ 调料 → **涉油安全卡（cookMethod 含煎炒炸淋油时自动展开）** → 步骤 → 关联原理（`<details>` 可展开）→ 改造版 → 搞砸补救 → 小贴士 → 「这次怎么样」记录（localStorage）
- 主料标签可点跳食材卡，食材卡可跳菜谱，双向联动
- 搜索按当前 tab 过滤；**切 tab 清空搜索词**（否则跨 tab 残留导致"切过去什么都没了"）

## 关键决策记录

- **库存功能：砍掉**。手动维护的库存状态两周后必然失真（"保质期死亡"）。用户否决过，别再复活
- **变体机制：C 方案（原理组合）为学习本质，B 轻量版为呈现**——改造版注记挂主菜谱，不写独立变体菜谱。用户实证认可（蛤蜊处理好→查蒸蛋→加料）
- **外部菜谱只能当骨架参考**：[老乡鸡 CookLikeHOC](https://github.com/Gar-b-age/CookLikeHOC) 是餐厅配方（克/温度 170℃/商业酱料包/步骤 4 行），教学层 100% 抄不了；但其菜名清单 = 菜谱扩张方向，步骤顺序 = 可靠做法逻辑
- **下一参考源**：[HowToCook（程序员做菜指南）](https://github.com/ialley-workshop-open-collection/HowToCook)——面向做饭小白的开源菜谱，颗粒度一致，比老乡鸡对口
- **量基准**：家常量 2-3 人份、一锅吃两顿（盖浇饭），不做一人份缩量（小份反而难做+启动成本摊薄+容错率更高）
- 用户灶具：燃气灶（火候教火焰语言）；一口不粘锅一口铁锅（菜谱必须指定锅具）

## 本轮交付（2026-08-22）

- `knowledge.js` 新建：9 原理卡（去腥/火候三档/熟没熟/复热/生熟比/涉油安全/活物先麻醉/味道验收三问/口感目标）+ 12 食材卡（含新鲜度/储存/预处理/生熟比/简单做法）
- `recipes.js`：白灼虾、蒸水蛋改造为「状态优先+权威口径」样板（蛋壳量水法）；新增 4 道家常菜（宫保鸡丁四关模板、农家小炒肉、鱼香肉丝、虾仁蒸鸡蛋+蛤蜊蒸蛋变体）
- `index.html` 重写：三 tab、预演头、涉油卡、权威口径、改造版、记录/flag、双向联动
- `sw.js` → v2（加 knowledge.js）
- 启动脚本 + BOM 修复
- 备份：`C:\Users\log\Desktop\AI\backup\kitchen-assistant-20260822-131415`

## 使用方式

1. 手机浏览器打开 **https://loggg4823-create.github.io/kitchen-assistant/**（公开仓库 + GitHub Pages，免费）
2. 菜单「添加到主屏幕」→ 离线可用（HTTPS 下 SW 真离线生效）
3. 内容更新：改完 `git push origin master` 即上线；**每次更新顺手 bump `sw.js` 的 `CACHE_NAME`**，手机下次打开自动换缓存
4. 本地开发预览：双击 `start-server.cmd`（电脑/手机同 WiFi 局域网访问）

## 已知坑（踩过）

- **GitHub Pages 私有仓库免费计划不支持**（2025 政策变更）——本仓库因此为公开；内容无隐私，接受
- `start-server.ps1` 有 BOM 但内容是**固化乱码**（GBK 误读后存回 UTF-8，`鍘ㄦ埧`）——只影响控制台提示，不影响功能；重写注释时整体重存
- SW cache-first：改代码后开发时清缓存或升 CACHE_NAME 版本
- 手机分类栏滑动曾反馈异常，已确认可用（若复发：`-webkit-overflow-scrolling:touch` + `touch-action:pan-x`）
- 测试环境：`playwright-cli open --browser=msedge`（本机无 Chrome）

## 下一步（未完成事项）

1. log 手机实测宫保鸡丁（锚点串已就绪，等实战反馈驱动迭代）
2. 家常菜持续扩张（按 HowToCook + 用户场景：西兰花炒鸡胸肉、酸辣土豆丝等；**主线=猪/鸡炒菜骨架**，海鲜是边缘场景）
3. 同门菜差异注记（「骨架同小炒肉，差异：…」）——记忆锚点用顺手后讨论
4. 可选生活知识（未入工具）：冷冻分装米饭+微波复热（用户已接受方案）
